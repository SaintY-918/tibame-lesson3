# 測試專用環境變數（僅 DATABASE_URL 需要覆蓋）
# 其餘機敏值（JWT_SECRET、COOKIE_SECRET 等）由 loadDotenv.ts 從根目錄 .env 補齊
# ⚠️ 不要在此設 VITE_ 前綴的變數
DATABASE_URL=postgresql://vms:vms@localhost:5432/vms_test?schema=public
NODE_ENV=test
